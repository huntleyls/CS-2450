#include <stdio.h>

int printTriangle(int);
int printRectangle(int, int);
int printFormat(int,int, int);

int main() {
      printTriangle(10);
      printRectangle(10,2);
      printFormat(30,1,2);
      
      return 0;
      
}
